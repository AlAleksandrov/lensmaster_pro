.equipment-checkbox-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 8px;
}

.equipment-checkbox-list li {
    background: #1a1a1a;
    border: 1px solid #444;
    border-radius: 6px;
    padding: 8px 12px;
}

.equipment-checkbox-list li label {
    color: #ccc;
    font-size: 0.85rem;
    cursor: pointer;
    margin-left: 8px;
}

.equipment-checkbox-list li input[type="checkbox"]:checked + label {
    color: #fff;
    font-weight: bold;
}