services:
  - type: web
    name: lensmaster_pro
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn lensmaster_pro.wsgi"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lensmaster_pro_render_db
          property: connectionString